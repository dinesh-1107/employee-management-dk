apiVersion: apps/v1
kind: Deployment
metadata:
  name: resource-template
  namespace: employee-management
spec:
  template:
    spec:
      containers:
      - name: app
        resources:
          requests:
            cpu: "250m"
            memory: "256Mi"
          limits:
            cpu: "500m"
            memory: "512Mi"
