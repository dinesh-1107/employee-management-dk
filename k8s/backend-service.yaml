apiVersion: v1
kind: Service

metadata:
  name: employee-backend-service

spec:
  selector:
    app: employee-backend

  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
      nodePort: 30080

  type: NodePort